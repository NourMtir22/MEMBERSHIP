{% extends 'base.html.twig' %}

{% block title %}Highscores{% endblock %}

{% block body %}
    <h1>Highscores</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Ids</th>
                <td>{{ highscore.ids }}</td>
            </tr>
            <tr>
                <th>Score</th>
                <td>{{ highscore.score }}</td>
            </tr>
            <tr>
                <th>IdUser</th>
                <td>{{ highscore.idUser }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_highscores_index') }}">back to list</a>

    <a href="{{ path('app_highscores_edit', {'ids': highscore.ids}) }}">edit</a>

    {{ include('highscores/_delete_form.html.twig') }}
{% endblock %}
