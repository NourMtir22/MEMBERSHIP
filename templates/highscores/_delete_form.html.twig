<form method="post" action="{{ path('app_highscores_delete', {'ids': highscore.ids}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ highscore.ids) }}">
    <button class="btn">Delete</button>
</form>
