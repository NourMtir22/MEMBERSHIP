{% extends 'base.html.twig' %}

{% block title %}Cartefidelite{% endblock %}

{% block body %}
    <h1>Cartefidelite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdCf</th>
                <td>{{ cartefidelite.idCf }}</td>
            </tr>
            <tr>
                <th>Pointmerci</th>
                <td>{{ cartefidelite.pointmerci }}</td>
            </tr>
            <tr>
                <th>Dateexpiration</th>
                <td>{{ cartefidelite.dateexpiration ? cartefidelite.dateexpiration|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>IdUser</th>
                <td>{{ cartefidelite.idUser }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cartefidelite_index') }}">back to list</a>

    <a href="{{ path('app_cartefidelite_edit', {'idCf': cartefidelite.idCf}) }}">edit</a>

    {{ include('cartefidelite/_delete_form.html.twig') }}
{% endblock %}
